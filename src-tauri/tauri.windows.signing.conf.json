{
  "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
  "bundle": {
    "windows": {
      "signCommand": {
        "cmd": "pwsh",
        "args": [
          "-NoProfile",
          "-ExecutionPolicy",
          "Bypass",
          "-Command",
          "$ErrorActionPreference = 'Stop'; $candidates = @('scripts/sign-windows.ps1', '../scripts/sign-windows.ps1'); $script = $candidates | Where-Object { Test-Path -LiteralPath $_ } | Select-Object -First 1; if (-not $script) { throw \"Could not find scripts/sign-windows.ps1 from $PWD\" }; & (Resolve-Path -LiteralPath $script).Path \"%1\""
        ]
      }
    }
  }
}
